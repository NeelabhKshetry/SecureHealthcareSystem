@model IEnumerable<HealthcareSystemDesign.Models.BillingType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BillingName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Billing)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BillingName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Billing.BillingId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BillingtypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BillingtypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BillingtypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
